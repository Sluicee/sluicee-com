# URL к PocketBase API
VITE_POCKETBASE_URL=http://127.0.0.1:8090

# last.fm-ключ дёргается прямо из браузера (у API открытый CORS) —
# поэтому нужен префикс VITE_, иначе Vite не положит его во фронтенд-бандл
VITE_LASTFM_API_KEY=

# Эти два — только для PocketBase-хука (letterboxd/youtube cron), в браузер не идут
YOUTUBE_API_KEY=
FANART_API_KEY=
